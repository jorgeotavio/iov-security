version: '3.8'

services:
  app:
    build: .
    shm_size: '1gb'
    volumes:
      - .:/usr/src/app
    # deploy:
    #   resources:
    #     reservations:
    #       devices:
    #       - driver: nvidia
    #         count: 1
    #         capabilities: [gpu]
    # command: python src/generate_images_for_shufflenet.py
    command: python src/train_model.py